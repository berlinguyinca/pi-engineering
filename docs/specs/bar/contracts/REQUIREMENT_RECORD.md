# Requirement Record Contract
Required fields: id, project, statement, source provenance (file/section/hash when available), dependencies, evidence requirements, current state, source mappings, runtime mappings, tests, artifacts, verifier identity, timestamps, blockers, repair campaign IDs. Requirements start UNKNOWN. Never infer VERIFIED from source presence.
