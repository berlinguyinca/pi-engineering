# Audit Report Contract
Report counts for every state; never collapse UNKNOWN/PARTIAL/BLOCKED into PASS. Include source revision, runtime environment, audit coverage, untested surfaces, top root-cause clusters, dependency ordering, generated campaigns, before/after deltas, and exact next action. Percent verified denominator must be explicit.
