# Baseline Contract
Before repair, persist an immutable audit baseline containing requirement matrix, source revision, environment fingerprint, service/topology inventory, CAV results, screenshots, traces, console/network errors, logs, performance/soak observations, visual findings and generated defect ledger. Baselines are append-only and addressable by audit ID.
