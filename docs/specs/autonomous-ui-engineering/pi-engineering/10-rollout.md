# Rollout

Stage rollout: shadow measurement -> advisory specs -> automatic worktree remediation -> low-risk deterministic auto-accept -> bounded autonomous improvements -> optional repository-policy auto-merge.

Always retain baselines, provenance and rollback. Existing behavioral tests remain authoritative; UI quality tooling augments them.
