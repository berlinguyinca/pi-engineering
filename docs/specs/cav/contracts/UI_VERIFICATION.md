# UI Verification Contract

UI diffs automatically require browser verification. Required capture: page exceptions, console errors, failed requests/4xx/5xx as policy defines, interaction assertions, relevant state transitions, traces, and screenshots. Visual requirements require reference comparison at specified viewports. Vision review may add defects but may never override deterministic failures. Include loading, empty, error, stale-data, keyboard/focus and responsive states where applicable.
