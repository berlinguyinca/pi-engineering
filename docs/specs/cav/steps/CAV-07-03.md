# CAV-07-03 — Visual Regression: Add deterministic tests

## Objective
Golden screenshots, viewport matrix, diffs and protected references.

This atomic step exists to **add deterministic tests**. Keep the change small, inspectable, reversible, and evidence-producing.

## Prerequisite
- CAV-07-02

## Required work
1. Reconcile current repository behavior against MASTER.md and this step before editing.
2. Implement only the minimum shared mechanism needed for this step; prefer deterministic code/tools over model judgment.
3. Add machine-readable evidence and stable requirement IDs for every new gate.
4. Add or update tests without weakening pre-existing acceptance criteria.
5. Run the real verification commands required by this step and retain artifacts.
6. Update documentation/configuration only after behavior is proven.

## Acceptance criteria
- The mechanism is observable through a deterministic test or artifact.
- A deliberately failing case fails closed and cannot be reported as VERIFIED.
- Existing trusted gates continue to pass.
- No protected acceptance/golden artifact is silently rewritten to make the implementation pass.
- The ledger can explain why this step is PASS/FAIL/UNKNOWN from evidence, not model prose.

## Exit gate
Record `CAV-07-03=VERIFIED` only after the acceptance criteria have executable evidence. If any criterion is UNKNOWN, stop this phase and repair it before proceeding.

## Handoff to next step
The next worker must begin by reading MASTER.md, ROADMAP.md, this step's evidence, and the next numbered step. It must not assume success from chat history.
