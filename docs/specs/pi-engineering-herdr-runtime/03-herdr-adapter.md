# 03 — Herdr Adapter
Perform a compatibility spike against actual current Herdr and pi-herdr versions/APIs. Record lifecycle fidelity, structured errors, worktrees, recovery, remote behavior, Pi compatibility and testability. Prefer pi-herdr where stable; otherwise use supported Herdr automation APIs. Never fork Herdr. Implement HerdrAgentRuntime plus explicit version/capability negotiation.
