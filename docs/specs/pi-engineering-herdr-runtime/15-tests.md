# 15 — Test Plan
Automate AgentRuntime contracts, Herdr integration, mock runtime, multi-worker DAG, worktrees, service workers, structured-result enforcement, fan-out/synthesis, context/request budget + 413 regression, review/repair bounds, InferWeave admission handling, remote reconnect, coordinator/Herdr restart, lost worker, partial Git mutation, idempotent replay, security/path/secret tests, Pi-Web APIs/events and metric correlation.

Run a canary: planner → two parallel engineers → tester → independent reviewer → repair if needed → re-review. Demonstrate isolation, artifacts, events, InferWeave routing, visibility, restart reconciliation and final integration.
