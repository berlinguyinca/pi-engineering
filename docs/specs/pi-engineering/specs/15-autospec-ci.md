# 15 AutoSpec & CI
AutoSpec owns spec/work-item policy and calls Pi public orchestration; no process supervision or memory backend. Substantial loop: plan -> optional Plannotator -> implement -> targeted tests -> broad tests -> fresh review -> fix -> rerun -> docs -> final diff/status -> commit/PR by policy -> OpenViking commit. CI/log/review/changelog data uses common event model.
