# Everyday memory verification — 2026-09-14

Scope: everyday private-memory commands/hooks and update-time connection setup
in the engineering harness, initially based on upstream commit `1716585` and
integrated with the subsequently accepted status-bar change `50ec1fb`. No portal,
Cognito, worker prompt or server schema changes are part of this feature.

## Automated evidence

- `npm run typecheck`: passed.
- `npm run lint`: passed.
- `npm test`: 420 passed, zero failed, one optional PostgreSQL test skipped.
- `npm run test:e2e`: package loads with 14 commands and six existing core tools.
- New strict transport tests preserve default worker fail-closed behavior while
  rejecting invalid authentication, malformed data, redirects and timeouts in
  interactive mode. No credential values or raw error bodies enter diagnostics.
- Event-handler tests cover explicit writes only, bounded ephemeral recall,
  current-request precedence, session reset, stale asynchronous results, token
  rotation, error recovery, short explicit searches and headless command output.
- Full extension integration uses a real local HTTP service: a note saved in
  one extension instance is recalled in a fresh instance; the generation guard
  remains present, automatic memory is not persisted as transcript messages,
  and invalid keys become visible failures.
- Engineering cache regression first failed on the old host/key, then passed
  after connection-identity invalidation was implemented.
- Setup tests cover masked rendering, private files, update detection, reuse of
  external keys, failed/cancelled setup, profile precedence, headless/RPC behavior,
  and changes to profile/environment/key files while validation is pending.

## Live use

Using the operator's already-configured key against
`https://viking.metabolomics.us`, `/remember` saved a uniquely identified
temporary note. A fresh interactive-memory controller retrieved it through
`before_agent_start` and inserted it through `context` before the current user
request. No automatic transcript write was emitted. The confirmed existing-key
setup path authenticated over HTTPS and recorded/rechecked update revisions in
a disposable local profile.

Only the generated temporary note was deleted afterward, through a narrowly
scoped operator cleanup that verified its ID and text before deletion. The
temporary setup profile was removed; the operator's real key and settings were
not changed by the test. No paid model call was used. These checks prove the
command/transport/context path, not that a model will always act on a note.

## Independent review

The reviewer examined the changed implementation, tests and installed SDK
context semantics. The initial review found three medium issues: short explicit
search terms were filtered out, headless setup lost its visible guidance, and
context overwrote disabled/missing-key status. All were reproduced with failing
tests and fixed. Recalled notes were also moved before the current user request,
and optimistic setup-state checks were added to avoid overwriting a newer
connection during asynchronous UI/network work.

The review is scoped to this feature delta and its interactions with existing
worker isolation. It is not a new audit of every unrelated repository module.
Final independent re-review: APPROVE; unresolved critical/high/medium/low counts
all zero. The reviewer independently ran 73 targeted tests and fresh diagnostics.

After the upstream status-bar change landed, CI exposed an incomplete UI test
fixture and the new custom footer's omission of extension statuses. The footer
now composes a width-bounded extension-status row, with memory first and the
primary model/throughput row unchanged. Real HTTP integration verifies that
readiness and rejected-key states are visible in the rendered footer. A separate
independent review approved this three-file integration delta with zero findings
and ten targeted tests plus diagnostics passing.

## Repository release evidence

`node scripts/dogfood-roadmap.ts` passed the incomplete → verified → invalidated
→ reverified lifecycle in a disposable repository. The existing manual roadmap
records predated accepted upstream changes and initially failed freshness checks.
New review/dogfood records must refer to the actual reviewed implementation
commit; deterministic checks are regenerated by the roadmap command. No gate,
required milestone or validation assertion is disabled for this feature.
