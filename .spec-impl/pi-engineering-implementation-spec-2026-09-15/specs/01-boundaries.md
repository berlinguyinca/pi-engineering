# 01 Boundaries & precedence
Create a gap matrix before refactoring. Enforce MASTER_SPEC ownership. Mark custom shared-Blackhole memory designs superseded. Prefer adapters/interfaces over cross-layer state. Acceptance: exactly one authoritative owner for worker lifecycle, shared memory, runtime state, plan approval, inference scheduling and UI state.
