# 16 Pi Forge integration
Inspect current Pi Forge before implementation. Reuse only capabilities that reduce duplication. Pi Web stays top-level UI and EventStore stays runtime truth. Never require users to manage the same project/run in two UIs. Integrate via supported adapter/API/deep-link/component. If Forge duplicates Pi Web, prefer Pi Web and document the decision.
