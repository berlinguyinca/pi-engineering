# 05 Work graph & scheduler
Persist Run/Task/Dependency/Worker graph. Schedule only dependency-ready tasks and account for write conflicts, role/project limits, provider capacity, host capacity, priority and budgets. Explicit blockers and waiting-for-human states. Compute best-effort critical path from evidence; never invent percentages or ETAs.
