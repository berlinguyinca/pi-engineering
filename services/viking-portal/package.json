{
  "name": "pi-viking-portal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node src/index.mjs",
    "test": "node --test test/*.test.mjs",
    "check": "../../node_modules/.bin/biome check --config-path=. . && node --check src/server.mjs && node --check src/index.mjs",
    "format": "../../node_modules/.bin/biome format --config-path=. --write ."
  },
  "dependencies": { "pg": "^8.13.1" }
}
